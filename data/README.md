# Data Directory

This directory contains datasets, schemas, and preprocessing scripts.

## Schemas
- `schemas/training_data.json`: Validator schema for fine-tuning data.
- `schemas/inference_request.json`: Validator for API requests.

## Samples
Sample data for testing the pipeline is available in `samples/`.
