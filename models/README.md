# Models Directory

This directory contains model checkpoints and exports.

## Checkpoints
`checkpoints/` stores training checkpoints managed by the `WildkatzeTrainer`.

## Exports
`exports/` stores quantized (INT8/4) and ONNX exported models for deployment.
